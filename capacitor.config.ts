import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'inno3fussion.template.app',
  appName: 'inno3fussion-template',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
